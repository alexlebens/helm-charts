suite: test Deployment generation
templates:
  - charts/valkey/templates/deploy_valkey.yaml
  - charts/valkey/templates/init_config.yaml
  - charts/valkey/templates/configmap.yaml
tests:
  - it: should generate a Deployment when replica mode is disabled (default)
    template: charts/valkey/templates/deploy_valkey.yaml
    asserts:
      - isKind:
          of: Deployment
      - hasDocuments:
          count: 1

  - it: should not generate a Deployment when replica mode is enabled
    set:
      valkey:
        replica:
          enabled: true
    template: charts/valkey/templates/deploy_valkey.yaml
    asserts:
      - hasDocuments:
          count: 0

  - it: should have exactly 1 replica in standalone mode (default)
    template: charts/valkey/templates/deploy_valkey.yaml
    asserts:
      - equal:
          path: spec.replicas
          value: 1

  - it: should configure resources in standalone mode (default)
    template: charts/valkey/templates/deploy_valkey.yaml
    asserts:
      - equal:
          path: spec.template.spec.containers[0].resources.requests.cpu
          value: 10m
      - equal:
          path: spec.template.spec.containers[0].resources.requests.memory
          value: 20Mi
