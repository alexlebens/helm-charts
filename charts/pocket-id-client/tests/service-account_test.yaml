suite: test Seed Job ServiceAccount
templates:
  - service-account.yaml
tests:
  - it: should render ServiceAccount by default
    release:
      name: my-app
      namespace: my-namespace
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: ServiceAccount
      - equal:
          path: metadata.name
          value: pocket-id-seed
      - equal:
          path: metadata.namespace
          value: my-namespace
      - equal:
          path: metadata.annotations["helm.sh/hook"]
          value: post-install
      - equal:
          path: metadata.annotations["helm.sh/hook-weight"]
          value: "-10"

  - it: should not render ServiceAccount when seedJob is disabled
    set:
      seedJob:
        enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: should not render ServiceAccount when create is false
    set:
      seedJob:
        serviceAccount:
          create: false
    asserts:
      - hasDocuments:
          count: 0

  - it: should respect custom serviceAccount name
    set:
      seedJob:
        serviceAccount:
          name: custom-seed-sa
    asserts:
      - equal:
          path: metadata.name
          value: custom-seed-sa
