
Garage Bucket "{{ include "garageBucket.name" . }}" has been deployed.

  Bucket:    {{ include "garageBucket.name" . }}
  Key:       {{ include "garageKey.name" . }}
  Cluster:   {{ .Values.garageCluster.name }} ({{ .Values.garageCluster.namespace }})
  Namespace: {{ .Release.Namespace }}

{{- if .Values.externalSecret.enabled }}

  Credentials are imported via ExternalSecret "{{ include "garageSecret.name" . }}"
  from ClusterSecretStore "{{ .Values.externalSecret.storeName }}".
{{- else }}

  Credentials will be auto-generated by the Garage Operator.
{{- end }}

{{- $backupList := list }}
{{- range $name, $cfg := .Values.backups }}
{{- if $cfg.enabled }}
{{- $backupList = append $backupList $name }}
{{- end }}
{{- end }}
{{- if $backupList }}

Backups:
{{- range $name, $cfg := .Values.backups }}
{{- if $cfg.enabled }}
  - {{ $name }}: {{ $cfg.cronJob.schedule }} ({{ $cfg.cronJob.timeZone }}) -> {{ $cfg.destination.bucketName }}
    {{- if $cfg.prune.enabled }} [prune after {{ $cfg.prune.ageToPrune }}]{{ end }}
{{- end }}
{{- end }}
{{- end }}

To verify the bucket is ready, run:

  helm test {{ .Release.Name }} --namespace {{ .Release.Namespace }}
